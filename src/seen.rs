// This module has been removed. Seen-IDs tracking now lives in `state::State`
// and is persisted as part of `state.json`. Keeping this stub file so historical
// references to it don't 404 if anything still links here; it is no longer
// included in the crate (no `mod seen;` in main.rs).
